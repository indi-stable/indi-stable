# No custom-library-search-path entry: -dev ships only headers and the
# unversioned .so symlink, which carries no RUNPATH of its own. If that tag
# ever appears for a -dev package, something has changed and it should be
# investigated, not blanket-overridden.
indi-stable-3rdparty-libs-apogee-dev: dir-or-file-in-opt
